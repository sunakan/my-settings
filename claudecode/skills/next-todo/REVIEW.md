# REVIEW: next-todo

> レビュー日時: 2026-05-07
> ファイル: /Users/user01/.claude/skills/next-todo/SKILL.md

## 総評
TODO.md の「検討中」から次の項目を取り出し、5項目（現状・課題/背景・対応手段・効果・期待）の決まった形式で提示するスキル。56行とコンパクトで責務が明確。`context: fork` を付けない理由が frontmatter コメントで明記されており、対話フローとの整合も取れている。frontmatter・本文ともに大きな問題はない。

## ✅ 良い点
- frontmatter L7 に `context: fork` を付けない理由がコメントで明記されている（Step 5 の確認がメイン会話で機能する必要があるため）。チェック [1] の `context` 観点で(B)に該当し、現在の設定（`context: fork` なし）が正しい。
- `disable-model-invocation: true`（L5）が設定されており、自動トリガーで TODO.md が勝手に編集されるのを防いでいる。
- `allowed-tools: Read Edit Bash(grep *)`（L6）と必要最小限に絞られている（チェック [4] 過剰許可なし）。
- `when_to_use`（L4）が独立フィールドとして存在し、使用タイミングが明確。
- TODO.md がない場合（Step 1）・「検討中」セクションが空 / 存在しない場合（Step 3）の2段階フォールバックが書かれている（チェック [2] フォールバック）。
- Step 2 の完了済み判定では、参照先のファイル状態と照合して機械的に判定し、判定が難しいときはスキップする（過剰削除しない）方針が明確。
- 提示する5項目（現状・課題/背景・対応手段・効果・期待）が具体的に定義されており、出力の一貫性が保たれる。
- Step 6 で作業完了後の TODO.md 整理ルールが明記されており、フローが閉じている。
- 削除範囲（`###` 見出しから次の `---` セパレータまで）が具体的に指定されている。
- 命令形（「〜する」「〜と返して終了する」）で書かれており、チェック [2] の文体要件を満たす。

## ⚠️ 改善提案（任意対応）
- **Step 5 の確認文言が複数候補**: 「進めますか？」または「どの手段で進めますか？」の使い分け基準が暗黙。Step 4 の「対応手段」で複数選択肢を提示した場合は後者、単一案なら前者、と明文化するとさらに迷いが減る（軽微）。
- **`Bash(grep *)` の用途**: `allowed-tools` に `Bash(grep *)` が含まれているが、本文中で `grep` を実行する箇所が見当たらない。実際に使用しないなら削除して権限を最小化することを検討。

## ❌ 問題点（要修正）
なし。

## 修正後の frontmatter サンプル（変更がある場合のみ）
（変更なし。ただし `Bash(grep *)` を使わないなら削除を検討）

# REVIEW: next-todo

> レビュー日時: 2026-05-07
> ファイル: /Users/user01/.claude/skills/next-todo/SKILL.md

## 総評
TODO.md の「検討中」から次の項目を取り出し、現状・課題/背景・対応手段・効果・期待の形式で提示してユーザーに確認を求めるスキル。57行とコンパクトで責務が明確。`disable-model-invocation: true` と最小限の `allowed-tools`、`context: fork` なし（理由コメントあり）が適切に設定されており、致命的な問題はない。

## ✅ 良い点
- frontmatter コメント（L7）に `context: fork` を付けない理由が明記されている（Step 5 の「進めますか？」確認がメイン会話で機能する必要があるため）。チェック [1] の `context` 判定は (B)「ユーザーへの会話が主目的」に該当し、現在の設定が正しい。
- `disable-model-invocation: true`（L5）が設定されており、TODO操作という副作用のある操作で自動invokeを防いでいる。
- `allowed-tools: Read Edit Bash(grep *)`（L6）と必要最小限に絞られており、`Bash` 全体許可になっていない（チェック [4] 過剰許可なし）。
- フォールバックが2箇所（L16: TODO.mdなし、L32: 検討中セクション空または不在）で明確に定義されており、チェック [2] フォールバック要件を満たす。
- Step 2 の「完了済み判定ロジック」（L20-L26）で過剰削除を防ぐ配慮がある（「判定が難しい場合はスキップ」）。
- Step 6 で作業完了後の TODO.md 整理ルールが明記されており、フローが閉じている。
- 削除範囲（`###` 見出しから次の `---` セパレータまで）が具体的に指定されている。
- 57行で500行制限を大幅に下回り、supporting files への分割は不要。
- `when_to_use`（L4）が独立フィールドとして存在し、呼び出し判断の文脈が明確。

## ⚠️ 改善提案（任意対応）
- **`Bash(grep *)` の用途が不明**: `allowed-tools` に `Bash(grep *)` が含まれているが、本文中で `grep` を使う箇所が見当たらない。実際に使用しないなら削除して権限を最小化することを検討（Step 2 で「参照ファイルが存在する場合は Read して」と書かれており、存在確認に `grep` を使うケースが想定されている可能性はあるが、`Read` ツールで事足りる）。
- **Step 5 の確認文言が複数候補で使い分け基準が暗黙**: 「進めますか？」または「どの手段で進めますか？」の使い分け基準が明示されていない。Step 4 の「対応手段」で複数選択肢を提示した場合は後者、単一案なら前者、と明文化するとより迷いが減る（軽微）。

## ❌ 問題点（要修正）
なし。

## 修正後の frontmatter サンプル（変更がある場合のみ）
（変更不要。ただし `Bash(grep *)` を実際に使わないなら削除を検討）

---
globs: "**/*.typ"
description: Typst ファイル編集時に適用する日本語技術書の文章ガイドライン
---

# 日本語技術書 文章ガイドライン

## 1. 読点は一文につき最大ひとつ

一文に読点「、」は最大ひとつに抑える。
読点が増えたら文を分割する。

```
### ❌ Bad
ただこのルールを守るのは大変で、普通に文章を書くと、自然と読点は2つ3つ出てきます。
これを回避するには、文をかなり洗練させる必要があります。
試してみると分かりますが、このルールは書き手に強烈な苦行を強います。
しかし文章を改善するメカニズムとしては優秀ですし、「読点が多くなったら書き換える」という単純なサイクルで、文章が劇的に改善されていきます。

### ✅ Good
ただこのルールを守るのは大変です。
普通に文章を書くと、自然と読点は2つ3つ出てきます。
これを回避するには、文をかなり洗練させる必要があります。
試してみると分かりますが、このルールは書き手に強烈な苦行を強います。
しかし文章を改善するメカニズムとしては優秀です。
「読点が多くなったら書き換える」という単純なサイクルで、文章が劇的に改善されていきます。
```

## 2. 一文のなかで同じ助詞は使わない

一文の中で同じ助詞を繰り返さない。
とくに「の」「を」の連続は言い換えや名詞化で回避する。

```
### ❌ Bad
- 設定の変更の影響の確認の手順を説明する。
- APIのレスポンスの形式を変更する対応を検討する。
- ユーザーを管理する機能を追加する対応を行う。

### ✅ Good
- 設定を変更した場合の影響確認手順を説明する。
- APIのレスポンス形式について、変更を検討する。
- ユーザーを管理するための機能を追加する。
```

## 3. コードを完全な状態で掲載する

コードは省略せず、写経してプッシュしたら動く完全な形で掲載する。

```
### ❌ Bad
// ハンドラ部分のみ抜粋
func hello(w http.ResponseWriter, r *http.Request) {
	fmt.Fprintln(w, "hello")
}

### ✅ Good
package main

import (
	"fmt"
	"net/http"
)

func hello(w http.ResponseWriter, r *http.Request) {
	fmt.Fprintln(w, "hello")
}

func main() {
	http.HandleFunc("/", hello)
	http.ListenAndServe(":8080", nil)
}
```

## 4. 着目すべきポイントへコメントで誘導する

コード中の着目箇所にはコメントを付けて視線を誘導する。

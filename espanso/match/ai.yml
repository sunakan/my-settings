# yaml-language-server: $schema=https://raw.githubusercontent.com/espanso/espanso/dev/schemas/match.schema.json

matches:
  - trigger: ";web"
    replace: "細かくWeb検索して精査して。事実と仮説・推測は分けて回答して"
  - trigger: ";pr"
    replace: "gh prでPR作成するコマンドを作って。アサインは自分、bodyは/tmp/において、ドラフトPRで"
